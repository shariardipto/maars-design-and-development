import Image from "next/image";
import Link from "next/link";
import type { SiteSettings } from "@/lib/config";
export default function Footer({ settings }: { settings: SiteSettings }) {
  const socials = [{label:"Instagram",href:settings.instagram},{label:"Facebook",href:settings.facebook},{label:"LinkedIn",href:settings.linkedin},{label:"Twitter",href:settings.twitter}].filter((s)=>s.href);
  return <footer className="site-footer"><div className="mddl-container"><div className="footer-contact"><div><h3>Phone:</h3>{settings.phone ? <a href={`tel:${settings.phone.replace(/[^+\d]/g,"")}`}>{settings.phone}</a> : <Link href="/contact">Start a conversation</Link>}</div><div><h3>Address:</h3><p>{settings.address || settings.tagline}</p></div><div><h3>Email:</h3>{settings.email ? <a href={`mailto:${settings.email}`}>{settings.email}</a> : <Link href="/contact">Contact the studio</Link>}</div><div><h3>{socials.length ? "Social:" : "Explore:"}</h3><div className="footer-links">{socials.length ? socials.map((s)=><a href={s.href} key={s.label} target="_blank" rel="noreferrer">{s.label}</a>) : <><Link href="/about">About</Link><Link href="/projects">Projects</Link><Link href="/services">Services</Link></>}</div></div></div><div className="footer-bottom"><Link href="/" aria-label={`${settings.siteName} home`}><Image src="/branding/logo.png" alt={settings.siteName} width={150} height={60} /></Link><p>&copy; {new Date().getFullYear()} {settings.siteName}. All rights reserved.</p><Link href="/admin">Studio login &#8599;</Link></div></div></footer>;
}
